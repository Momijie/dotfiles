Escape
    bash -c "~/.scripts/poly_escape"

{l, e, s, r, o}
    bash -c "~/.scripts/poly_escape" ; \
    {   dm-tool switch-to-greeter, \
        bspc quit, \
        systemctl suspend ; dm-tool switch-to-greeter, \
        systemctl reboot, \
        systemctl poweroff -i \
    }
